<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="ucl.group.excelSystem.api.db.dao.TimeOrderDao">

        <select id="findProjectsForStatusUpdate" resultType="ucl.group.excelSystem.api.db.pojo.TimeOrderEntity">
            SELECT project_id AS projectId,
            project_end_date AS projectEndDate,
            project_scheduled_end_date AS projectScheduledEndDate
            FROM basic_v2_project_manage
            WHERE active_flg IN (0, 1)
        </select>

        <update id="deactivateProjectById">
            UPDATE basic_v2_project_manage
            SET active_flg = 1
            WHERE project_id = #{projectId}
        </update>

        <update id="deactivateProjectDetailById">
            UPDATE basic_v2_project_detail
            SET active_flg = 1
            WHERE project_id = #{projectId}
        </update>

    </mapper>
